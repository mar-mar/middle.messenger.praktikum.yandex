{{#*inline "className"}}text-button{{/inline}}

<div class="{{> className}} {{#if red}}{{> className}}--red{{/if}}">
    
    <button onClick="window.location.href='{{pageUrl}}'">
        
        <span class="{{> className}}__label">{{label}}</span>
        
    </button>

</div>