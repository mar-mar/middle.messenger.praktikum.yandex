@import url("src/style/clear.pcss");
@import url("src/style/variable.pcss");

.root {
    display: inline-flex;
    color: var(--active-color);

    &__button {
        display: flex;
    }

    &--red {
        color: var(--red-font-color);
    }
}
