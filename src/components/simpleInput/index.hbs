
<input  class="{{ styles.root }}__input {{~#if parentClassName}} {{parentClassName}}__simple-input{{~/if~}}" 
    type="{{type}}"
    placeholder=" " autocomplete="off"
    value="{{value}}" 
    name="{{name}}"
>
