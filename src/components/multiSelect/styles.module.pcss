.root {
    overflow: auto;
    flex-grow: 1;
   
    &__label {
        display: flex;
        margin-bottom: var(--mini-offset);
    }

    &__checkbox {
        margin-right: var(--mini-offset);
    }

}
