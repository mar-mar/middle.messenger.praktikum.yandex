@import url("src/style/clear.pcss");
@import url("src/style/variable.pcss");

.root {
    color: var(--active-color);
    cursor: pointer;
    display: inline-flex;

    &__input {
        display: none;
    }
}
