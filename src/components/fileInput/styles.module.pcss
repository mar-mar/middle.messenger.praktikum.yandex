@import url("src/style/clear.pcss");
@import url("src/style/variable.pcss");

.root {
    cursor: pointer;
    display: inline-flex;
    color: var(--active-color);

    &__input {
        display: none;
    }
}
