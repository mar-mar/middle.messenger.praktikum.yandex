<div class="{{ styles.root }} {{#if isMine}}{{ styles.root--my }}{{/if}}">
                    
    <div>{{item.content}}</div>
    <div class="{{ styles.root-time}}">{{messageTime}} {{selectedChatId}} </div>
</div>
