.root {
    align-items: center;
    display: flex;
    padding: var(--mini-offset);

    &--nochat {
        display: none;
    }
    
    &__label {
        flex-grow: 1;
        margin-left: var(--micro-offset);
    }

}
