.root {
    display: flex;
    flex-direction: row;
    align-items: center;
    padding: var(--micro-offset);
    
    &__input-cont {
        margin: 0 var(--micro-offset);
        flex-grow: 1;
    }

    &__send-cont {
        padding: var(--micro-offset);
    }
}
