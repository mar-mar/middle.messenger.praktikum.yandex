.chat-list {

    &__header {
        display: flex;
        flex-direction: row;    
    }
    
    &__header__label{
        flex-grow: 1;
    }

    &__list {
        border-top: var(--normal-border-size) solid var(--border-color);
    }
}