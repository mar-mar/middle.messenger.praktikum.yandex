.root {
    display: flex;
    padding: var(--micro-offser);
    align-items: center;
    
    
    &__label {
        flex-grow: 1;
        margin-left: var(--micro-offser);
    }

    &__date {
       color: var(--grey-font-color);
       font-size: var(--mini-font-size);
    }
}
