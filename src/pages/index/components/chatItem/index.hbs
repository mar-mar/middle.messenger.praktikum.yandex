
<li class="{{ styles.root }} {{#if isSelected }}{{ styles.root--selected }}{{/if}}">

    <div class="{{ styles.root__avatar }}">
        {{{components_avatar imageUrl=getAvatar diameter='S'
            avatarOwner=chatItem.title }}}
    </div>

    <div class="{{ styles.root__label }}">
        {{chatItem.title}} 
    </div>

    <div class="{{ styles.root__date }}">
        {{getStrDate}}
        
    </div>
</li>

